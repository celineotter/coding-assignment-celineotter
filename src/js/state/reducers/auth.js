// import { actionTypes } from 'js/state/actions/auth';

const defaultState = {
  authenticated: null,
};

export default function (state = defaultState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
